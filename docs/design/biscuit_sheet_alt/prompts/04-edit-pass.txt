=== EDIT PASS (the form of words used to correct a panel in place) ===
Image 1 is the EDIT TARGET: a flat vector-style character drawing (white keyline, flat fills, near-black background) that must stay exactly as it is apart from the small corrections below. Use the built-in image_gen tool in EDIT mode on Image 1 (not a fresh generation), preserving the composition, the silhouette, the line work, every colour area and the background pixel-for-pixel as far as the tool allows.

Change ONLY these things:
1. EYES: remove every white catchlight, glint, reflection or highlight dot from the eyes. Each pupil becomes one flat dark disc (#3d2313) with no white in it; the amber iris stays one flat ring (#c4834e) with no gradient; the eye rim becomes a thin liver-brown line (#6b3f22), not black, the same width as the other interior lines. The eyes stay open, round, level and on the viewer.
2. NOSE: one flat liver-brown fill (#a9663b) — remove any lighter band, sheen or highlight across its top; nostrils and philtrum are white line only.
3. MOUTH (when it was drawn as a curve or a filled bar): one short flat horizontal white stroke, joined to the nose by a single thin white philtrum line.
[Per panel, one or two further lines named the specific defect the review had found — a missing stretch of keyline, interior feather strokes on the plume, darker fill inside the nostril loops, tan toe strokes — in the same change-only form.]

Keep everything else unchanged: same figure(s), same positions, same scale, same margins, same white keyline, same flat fills, same flat near-black background, no lettering, nothing added, nothing removed beyond what is listed.

Copy the edited PNG into the current working directory as out.png (an exact copy) and reply with only its absolute path.
